<Window x:Class="CrimesAndIncidents.Accomplices"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактировать список участников" Height="300" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <DataGrid Name="dataGrid" Margin="5,30,5,30"  Grid.ColumnSpan="2" MouseDoubleClick="dataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Звание" Binding="{Binding Path=Rank}"/>
                <DataGridTextColumn Header="ФИО" Binding="{Binding Path=ShortName}" Width="*"/>
                <DataGridTextColumn Header="Подразделение" Binding="{Binding Path=SubUnit}"/>
                <DataGridTextColumn Header="Воинская часть" Binding="{Binding Path=MilitaryUnit}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Name="btnAddItem" Content="Добавить" Margin="5,5,5,5" VerticalAlignment="Top" Click="btnAddItem_Click" />
        <Button Name ="btnDelete" Content="Удалить" Margin="0,5,5,5" Grid.Column="1" VerticalAlignment="Top" Click="btnDelete_Click" />
        <Button Name ="btnOk" Content="Ok" Margin="5,5,5,5" VerticalAlignment="Bottom" Grid.ColumnSpan="2" Click="btnOk_Click" />

    </Grid>
</Window>
