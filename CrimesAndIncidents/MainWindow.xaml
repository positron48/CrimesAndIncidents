<Window x:Class="CrimesAndIncidents.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Преступления и происшествия" Height="500" Width="900">
    <Window.Resources>
        <!-- datagid textblock -->
        <DataTemplate x:Key="temp1">
            <TextBlock Text="{Binding Item1}" TextWrapping="Wrap" />
        </DataTemplate>
        
    </Window.Resources>
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="14*"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
       
        
        <Menu  Margin="0,0,0,0" VerticalAlignment="Top" Background="White" >
            <MenuItem Header="Меню">
                <MenuItem Header="О программе"/>
                <MenuItem Header="Выход" Click="MenuExit_Click_1"/>
            </MenuItem>
            <MenuItem Header="Участики" Click="menuAccomplice_Click" />
            <MenuItem Header="Списки">
                <MenuItem Header="Звание" Click="editRank_Click"/>
                <MenuItem Header="Должность"  Click="MenuItem_Click_1"/>
                <MenuItem Header="Образование"  Click="MenuItem_Click_1"/>
                <MenuItem Header="Статья УК РФ" Click="editClause_Click_2" />
                <MenuItem Header="Категория"  Click="MenuItem_Click_1"/>
                <MenuItem Header="Кем призван"  Click="MenuItem_Click_1"/>
                <MenuItem Header="Кем возбуждено УД"  Click="MenuItem_Click_1"/>
                <MenuItem Header="Семейное положение"  Click="MenuItem_Click_1"/>
            </MenuItem>
            <MenuItem Header="Структура подразделений" Click="editStructureUnit_Click"/>
        </Menu>

        <ToolBar Margin="0,0,0,0" Grid.Row="1"  >
            <Button Name="btnAddCrimeOrIncidents" Margin="5,0,5,0" Content="Добавить" Height="25" ToolTip="Добавить" Click="btnAddCrimeOrIncidents_Click_1"/>
            <Button Name="btnDeleteCrimeOrIncidents" Margin="5,0,5,0" Content="Удалить" Height="25" ToolTip="Добавить" Click="btnDeleteCrimeOrIncidents_Click_1"/>
            
            <Label  Height="25" Margin="5,-4,0,0" Content="Выбрать с" />
            <DatePicker Name="dpLeft" Margin="0,-2,0,0" Height="25" CalendarClosed="dpLeft_CalendarClosed" LostFocus="dpLeft_LostFocus" />
            <Label  Height="25" Margin="0,-4,0,0" Content="по" />
            <DatePicker Name="dpRight" Margin="0,-2,5,0" Height="25" CalendarClosed="dpRight_CalendarClosed" LostFocus="dpRight_LostFocus"  />
            <Button Content="Ok" Name="btnOk" Click="btnOk_Click_1" />
            
        </ToolBar>

        <DataGrid Name="crimesDataGrid" Margin="0,0,0,0" AutoGenerateColumns="False" Grid.Row="2" MouseDoubleClick="crimesDataGrid_MouseDoubleClick" LoadingRow="crimesDataGrid_LoadingRow" >
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Учет" Binding="{Binding IsRegistred}"/>
                <DataGridTemplateColumn Header="Фабула" Width="*" MinWidth="150" SortMemberPath="Story">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Story}" TextWrapping="Wrap" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>                
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Совершено" Binding="{Binding Path=DateCommit}"  />
                <DataGridTextColumn Header="Возбуждено" Binding="{Binding Path=DateInstitution}" />
                <DataGridTextColumn Header="Учтено" Binding="{Binding Path=DateRegistration}"  />
                <DataGridTemplateColumn Header="Участники" SortMemberPath="Accomplice">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Accomplice}" TextWrapping="Wrap" TextAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>


                <DataGridTemplateColumn Header="УК" Width="75" SortMemberPath="NumberClause">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Clause}" TextWrapping="Wrap" TextAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            
                <!--
                    <DataGridTextColumn Header="Дата суда" Binding="{Binding Path=DateVerdict}"/>
                    <DataGridTextColumn Header="Решение суда" Binding="{Binding Path=Verdict}"/>
                -->
            </DataGrid.Columns>
        </DataGrid>
       

        <StatusBar Grid.Row="3" Grid.ColumnSpan="2">
            <TextBlock Name="statusBar" />
        </StatusBar>
    </Grid>
</Window>
